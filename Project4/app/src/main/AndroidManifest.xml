<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tushita.project4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Guest_Activity_homepage" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".Member_activity_homepage" />
        <activity android:name=".Add_a_book" />
        <activity android:name=".Profile" />
        <activity android:name=".Friends" />
        <activity android:name=".ReadingList" />
        <activity android:name=".BookList" />
        <activity android:name=".Member_sign_in" />
        <activity android:name=".Add_to_readinglst"></activity>
    </application>

</manifest>